/**
 * Return the first entry of an object, just like we sometimes do with an array.
 * @returns {Array} - [key, value]
 */
export default function obj_first_entry(obj: Record<any, any>): [any, any] | undefined;
